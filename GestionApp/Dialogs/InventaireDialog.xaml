<Window x:Class="GestionApp.Dialogs.InventaireDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Article" Height="520" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Annuler" Width="90" Padding="6" Margin="0,0,8,0" Style="{StaticResource OutlineButtonStyle}" Click="Cancel_Click"/>
            <Button Content="Enregistrer" Width="110" Padding="6" IsDefault="True" Click="Save_Click"/>
        </StackPanel>

        <StackPanel>
            <TextBlock Text="Nom *" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="NameBox" Margin="0,0,0,12" Padding="6"/>

            <TextBlock Text="Quantité, unité" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="QuantityBox" Grid.Column="0" Padding="6"/>
                <TextBox x:Name="UnitBox" Grid.Column="2" Padding="6"/>
            </Grid>

            <TextBlock Text="Catégorie" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="CategoryBox" Margin="0,0,0,12" Padding="6"/>

            <TextBlock Text="Seuil d'alerte (stock bas)" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="MinThresholdBox" Margin="0,0,0,12" Padding="6"/>

            <TextBlock Text="Notes" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="NotesBox" Padding="6" Height="70"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>
    </DockPanel>
</Window>
