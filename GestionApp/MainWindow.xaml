<Window x:Class="GestionApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GestionApp.ViewModels"
        mc:Ignorable="d"
        Title="Gestion App" Height="700" Width="1100" MinWidth="820" MinHeight="520">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{StaticResource HeaderBrush}" Padding="20,14">
            <DockPanel>
                <TextBlock Text="Gestion App" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <ToggleButton x:Name="NavMenuButton" Content="☰ Menu" Style="{StaticResource PillToggleButtonStyle}"
                                  IsChecked="{Binding IsNavPopupOpen}" Margin="0,0,10,0"/>
                    <Button Content="Actualiser" Style="{StaticResource OutlineButtonStyle}" Command="{Binding RefreshCommand}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <StatusBar DockPanel.Dock="Bottom" Background="{StaticResource NavBackgroundBrush}">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource NeutralDarkBrush}"/>
            </StatusBarItem>
        </StatusBar>

        <Border DockPanel.Dock="Left" Width="230" Background="{StaticResource NavBackgroundBrush}"
                BorderBrush="{StaticResource NavBorderBrush}" BorderThickness="0,0,1,0">
            <TreeView x:Name="NavTree" ItemsSource="{Binding NavItems}" BorderThickness="0" Background="Transparent"
                      SelectedItemChanged="NavTree_SelectedItemChanged" Margin="6,10,6,10">
                <TreeView.Resources>
                    <Style TargetType="{x:Type TreeViewItem}">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                        <Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BoolToVisibility}}"/>
                        <Setter Property="Padding" Value="8,7"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TreeViewItem}">
                                    <StackPanel>
                                        <Border x:Name="RowBorder" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <ToggleButton x:Name="Expander" Grid.Column="0" Width="16" Margin="0,0,4,0"
                                                              ClickMode="Press" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                                              Visibility="{Binding HasItems, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibility}}">
                                                    <ToggleButton.Template>
                                                        <ControlTemplate TargetType="ToggleButton">
                                                            <TextBlock Text="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ExpandedToChevron}}" FontSize="10" Foreground="{StaticResource NeutralBrush}"/>
                                                        </ControlTemplate>
                                                    </ToggleButton.Template>
                                                </ToggleButton>
                                                <Ellipse Grid.Column="1" Width="8" Height="8" Margin="0,0,8,0" VerticalAlignment="Center"
                                                         Fill="{Binding AccentBrush}"/>
                                                <ContentPresenter Grid.Column="2" ContentSource="Header" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ItemsPresenter x:Name="ItemsHost" Margin="18,0,0,0"/>
                                    </StackPanel>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsExpanded" Value="False">
                                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="RowBorder" Property="Background" Value="{StaticResource AccentBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="RowBorder" Property="Background" Value="{StaticResource NavHoverBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TreeView.Resources>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Title}"/>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>

        <Grid>
            <ContentControl Content="{Binding CurrentViewModel}" Margin="16"/>

            <Popup IsOpen="{Binding IsNavPopupOpen}" StaysOpen="False" AllowsTransparency="True"
                   PlacementTarget="{Binding ElementName=NavMenuButton}" Placement="Bottom" HorizontalOffset="-240" VerticalOffset="6">
                <Border Background="White" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="10"
                        Padding="16" Width="360" MaxHeight="500">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="18" ShadowDepth="4" Opacity="0.18" Color="Black"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Personnaliser le menu" FontWeight="Bold" FontSize="15" Margin="0,0,0,4"/>
                        <TextBlock Text="Affiche, masque ou réordonne les onglets." FontSize="11" Opacity="0.65" Margin="0,0,0,10" TextWrapping="Wrap"/>
                        <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding NavItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <CheckBox Grid.Column="0" Content="{Binding Title}" IsChecked="{Binding IsVisible}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                <Button Grid.Column="1" Content="↑" Width="26" Height="26" Padding="0" Margin="0,0,2,0"
                                                        Style="{StaticResource OutlineButtonStyle}"
                                                        Command="{Binding DataContext.MoveNavUpCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}"/>
                                                <Button Grid.Column="2" Content="↓" Width="26" Height="26" Padding="0"
                                                        Style="{StaticResource OutlineButtonStyle}"
                                                        Command="{Binding DataContext.MoveNavDownCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}"/>
                                            </Grid>
                                            <ItemsControl ItemsSource="{Binding Children}" Margin="24,4,0,0">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <CheckBox Content="{Binding Title}" IsChecked="{Binding IsVisible}" Margin="0,2" Opacity="0.85"/>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </StackPanel>
                </Border>
            </Popup>
        </Grid>
    </DockPanel>
</Window>
